{
  "name": "@electron/bugbot-runner-integration-tests",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "run-p -s",
    "test": "jest --runInBand",
    "test:ci": "jest --runInBand --coverage"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "jest": "^26.6.3",
    "npm-run-all": "^4.1.5",
    "object-hash": "^2.2.0",
    "uuid": "^8.3.2"
  }
}
